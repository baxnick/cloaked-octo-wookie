module Cloaked
  module Octo
    module Wookie
      class Patternizer
        #take a list of potentially unrelated files
        #exclude their extensions
        #group by same prefix
        #if a prefix == one item, that is the root item, the group was successful
        #else move onto the next item
      end
    end
  end
end
